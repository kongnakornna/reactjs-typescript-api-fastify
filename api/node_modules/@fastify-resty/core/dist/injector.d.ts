import type { Constructable, IInjectToken } from './types';
export declare const serviceTokens: Map<IInjectToken, Constructable>;
export default class Injector {
    private readonly injectableMap;
    private resolve;
    getInstance<T>(constructor: Constructable<T>): T;
    registerInstance(key: Constructable | IInjectToken, value: any): void;
}
